<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.appwork.lufthasnaproject"
    android:installLocation="internalOnly">

    <application
        android:name=".app.MyApp"
        android:allowBackup="false"
        android:fullBackupOnly="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Todo">
        <activity android:name=".ui.views.AnotherActivity"/>
        <activity android:name=".ui.views.LifeCycleActivity"/>
        <activity android:name=".ui.views.AddTodoActivity" />
        <activity android:name=".ui.views.TodoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>